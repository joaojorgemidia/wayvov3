/**
 * Regras de multa/juros pré-programadas no sistema.
 * Aplicadas como padrão em qualquer empresa quando o lançamento ou a locação
 * não tem valores específicos configurados.
 */
export const DEFAULT_MULTA_ATRASO = 15;     // R$ fixos por atraso
export const DEFAULT_JUROS_DIARIO = 7;       // R$ por dia de atraso
